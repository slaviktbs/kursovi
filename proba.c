#include <stdio.h>
#include <math.h>

int main(void) {
    double number = 9.0; // Predefined number
    double result;

    result = sqrt(number); // Calculate the square root
    printf("The square root of %lf is %lf\n", number, result);

    return 0;
}